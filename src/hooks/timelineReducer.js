const calculatorReducer = (state, action) => {
  switch (action.type) {
    
    case 'ADD': 
    
    case 'SUBTRACT': 
      case 'LOADING':

    default:
      return state;      
    }
  }
  